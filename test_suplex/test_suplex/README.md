# Suplex Test Suite

## Auth Testing
Nav to test_suplex folder and run `reflex run` to get a single page Reflex app for testing auth features. In the root folder, use .env.example to create your .env file pointing to your supabase instance.